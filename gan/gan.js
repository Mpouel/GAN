function gm() {
    if (document.getElementById("connect").innerHTML === "Disconnect") {
        console.log('Get Moves');
    }
}

function reset() {
    if (document.getElementById("connect").innerHTML === "Disconnect") {
        console.log('Reset state');
    }
}